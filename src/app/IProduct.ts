export interface IProduct{

    productId: number,
    productName: string,
    description: string,
    imageUrl: string,
    productCode: number,
    releaseDate: string,
    price: number,
    starRating: number

}